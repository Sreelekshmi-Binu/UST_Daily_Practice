package com.Int.ust.bean;

public class CredentialsBean {
	public static boolean isValidCredentials(String userName, String password) {
 
        if ("AD-001".equals(userName) && "password1".equals(password)) {
            return true;
        } else if ("AD-002".equals(userName) && "password2".equals(password)) {
            return true;
        } else if ("AD-003".equals(userName) && "password3".equals(password)) {
            return true;
        } else if ("AD-004".equals(userName) && "password4".equals(password)) {
            return true;
        }
        return false;
    }
}
